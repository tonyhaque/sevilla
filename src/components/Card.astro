---
interface Props {
	top?: boolean;
}
const { top = false } = Astro.props;
---

<div
	class={`${top ? "m-4 border-orange-500 border-3" : "border-emerald-500 my-4 border-2"} p-4 border rounded-r-xl`}
>
	<slot />
</div>
